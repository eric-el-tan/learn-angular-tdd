export class AcademicStandardisedRate {
  grade: string;
  level: number;
  scaledSalary: number;
}

export class ProfessionalStandardisedRate {
  band: string;
  minimumSalary: number;
  maximumSalary: number;
  constructor() {}
}
